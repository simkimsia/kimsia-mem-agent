# on vps at 2026-03-16 15:00

## rebuild image

```bash
cd ~/projects/kimsia-mem-agent
git pull
docker build -t kimsia-mem-agent:latest .
```

## clean stale state and rerun

```bash
cd ~/projects/mem-comp-26/harness
sudo docker ps -aq --filter "name=^/memcomp-" | xargs -r sudo docker rm -f
sudo rm -rf results/kimsia_glm47_mem_v1 workdir/*
sudo -E "$(pwd)/.venv/bin/python" main.py | tee run_kimsia_glm47_mem_v1.log
```
