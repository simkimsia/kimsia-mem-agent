```bash
# vps
cd ~/projects/kimsia-mem-agent
git pull
docker build --no-cache -t kimsia-mem-agent:latest .
```

```bash
cd ~/projects/mem-comp-26/harness
cp projects.json projects.json.bak
python3 - <<'PY'
import json
p=json.load(open('projects.json'))
json.dump([[p[0][0]]], open('projects.json','w'), indent=2)
PY

cat > candidates.json <<'JSON'
[
  {
    "run_name": "kimsia_smoke",
    "agent_docker_image": "kimsia-mem-agent:latest",
    "llm_quota_total": 200,
    "llm_quota_instance": 2,
    "enable_memory": true,
    "timeout_s": 7200,
    "env": { "MODEL_NAME": "glm-4.7" }
  }
]
JSON

sudo docker ps -aq --filter "name=^/memcomp-" | xargs -r sudo docker rm -f
rm -rf results/kimsia_smoke workdir/*
sudo -E "$(pwd)/.venv/bin/python" main.py | tee run_kimsia_smoke.log
```
